meta {
  name: update
  type: http
  seq: 4
}

put {
  url: {{url_api}}/org
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Computers SA",
    "slug": "computers-sa"
  }
}
